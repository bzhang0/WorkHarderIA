<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/back_deadlift"
        android:title="deadlift"/>

    <item android:id="@+id/back_latpulldown"
        android:title="lat pulldown"/>

    <item android:id="@+id/back_pullup"
        android:title="pull up"/>

    <item android:id="@+id/back_row"
        android:title="row"/>

    <item android:id="@+id/back_shoulderpress"
        android:title="shoulder press"/>

    <item android:id="@+id/back_shrug"
        android:title="shrug"/>
</menu>